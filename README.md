# BookStore
Bookstore API — Clean Architecture with .NET 10  RESTful bookstore API with custom Generic Repository + Unit of Work on EF Core. 4 layers: Domain, Application, Infrastructure, API. Explicit Fluent API, DTOs in API layer, LocalDB database.
